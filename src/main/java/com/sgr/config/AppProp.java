package com.sgr.config;

import com.sgr.models.FileInfo;

import java.util.concurrent.ConcurrentHashMap;

public class AppProp {
    public static ConcurrentHashMap<String, FileInfo> FileData =new ConcurrentHashMap<>();
}
